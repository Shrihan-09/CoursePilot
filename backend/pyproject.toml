[project]
name = "coursepilot-backend"
version = "0.1.0"
description = "CoursePilot backend: FastAPI application, deterministic academic core."
readme = "README.md"
# 3.12 is the floor. No upper cap: the application code runs fine on 3.14.
# The database drivers are the only 3.14 risk, so they live in the `postgres`
# extra rather than gating the whole package. The Docker image pins 3.12.
requires-python = ">=3.12"

dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.32",
    "pydantic>=2.9",
    "pydantic-settings>=2.6",
    "sqlalchemy[asyncio]>=2.0.36",
    "alembic>=1.14",
    "python-json-logger>=2.0",
]

[project.optional-dependencies]
# Split out because psycopg/pgvector wheel availability lags new Python
# releases. Everything except an actual database connection works without it.
postgres = [
    "psycopg[binary,pool]>=3.2",
    "pgvector>=0.3.6",
]
# The live explanation provider. Optional so a fresh clone, CI, and every
# ordinary test run work with no vendor package and no API key - the
# deterministic path is the default, not a fallback nobody exercises.
ai = [
    "anthropic>=0.40",
    "openai>=1.60",
]
dev = [
    "pytest>=8.3",
    "pytest-asyncio>=0.24",
    "httpx>=0.27",
    "ruff>=0.7",
    "mypy>=1.13",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]

[tool.ruff]
line-length = 100
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.mypy]
python_version = "3.12"
strict = true
plugins = ["pydantic.mypy"]

[tool.pytest.ini_options]
testpaths = ["tests"]
asyncio_mode = "auto"
# The conftest design rule says database-backed tests "opt in explicitly and
# are marked `db`". Registering the marker is what makes that real rather
# than a comment - an unregistered mark is only a warning.
markers = [
    "db: requires a PostgreSQL database (DATABASE_URL_SYNC)",
]
filterwarnings = [
    # Warnings are errors: an unnoticed DeprecationWarning today is a broken
    # build on the next dependency bump.
    "error",
    # Exception: the repo lives under OneDrive, which blocks the atomic
    # directory rename pytest uses to write .pytest_cache. That warning is
    # about the filesystem, not about our code.
    "ignore::pytest.PytestCacheWarning",
]
