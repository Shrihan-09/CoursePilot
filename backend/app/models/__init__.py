"""SQLAlchemy ORM models.

INTENTIONALLY EMPTY. The schema is designed in docs/DATA_MODEL.md but not yet
implemented — it should be reviewed against authoritative Rutgers requirement
structures before it becomes migrations, because guessing the shape of Rutgers
degree requirements and then migrating away from a wrong guess is expensive.

When models are added, import them all here so Alembic's autogenerate sees
them via `Base.metadata`.
"""
