# Major skills

One directory per Rutgers program, e.g. `majors/computer-science/`.

Each contains `skill.yaml` + `SKILL.md`. See ../SKILL_FORMAT.md.

NOT IMPLEMENTED YET. No major skills have been authored, because doing so
requires authoritative Rutgers program documentation that has not been
ingested. Writing a plausible-looking CS requirement skill from memory would
create exactly the unverifiable-claim problem the architecture forbids.

TODO(rutgers-source): obtain official program requirement documentation before
authoring any skill in this directory.
